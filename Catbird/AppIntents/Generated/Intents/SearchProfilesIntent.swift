// GENERATED by app_intents_generator.py — DO NOT EDIT
// Source: app.bsky.actor.searchActors
import Foundation
import AppIntents
import Petrel

@available(iOS 18.0, *)
struct SearchProfilesIntent: AppIntent {
    static let title: LocalizedStringResource = "Search Profiles"
    static let description = IntentDescription("Find Bluesky profiles matching a search query.")

    @Parameter(title: "Account")
    var account: AccountEntity?

    @Parameter(title: "Search Query")
    var q: String

    @Parameter(title: "Result Limit")
    var limit: Int?


    init() {}

    func perform() async throws -> some IntentResult & ReturnsValue<[ProfileEntity]> & ProvidesDialog {

        let did = account?.id ?? IntentAccountResolver.activeDID()
        let client = try await IntentClientProvider.shared.client(for: did)

        let params = AppBskyActorSearchActors.Parameters(q: q, limit: limit.map { min(max($0, 1), 100) })

        let output = try unwrapIntentResponse(await client.app.bsky.actor.searchActors(input: params))
        let value = output.actors.map { ProfileEntity(from: $0) }
        let dialog: IntentDialog
        if let first = value.first {
            dialog = IntentDialog(stringLiteral: "Found \(value.count) \(value.count == 1 ? "profile" : "profiles"). First: \(String(localized: first.displayRepresentation.title)).")
        } else {
            dialog = IntentDialog(stringLiteral: "No profiles found.")
        }
        return .result(value: value, dialog: dialog)
    }
}